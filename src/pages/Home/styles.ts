import { Box } from '@material-ui/core';
import styled from 'styled-components';

export const Layout = styled(Box)`
  display: block;
  width: 100%;
  height: 100%;
  place-items: center;
  flex-flow: column;
`;
